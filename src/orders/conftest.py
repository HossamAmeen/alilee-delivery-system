"""App-level pytest config for the orders app.

Keep this file minimal — top-level fixtures defined in `src/conftest.py` are
automatically available to tests here. Add app-specific fixtures if needed.
"""

import pytest

__all__ = ["pytest"]
